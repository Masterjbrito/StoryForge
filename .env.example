# ===================================================================
# StoryForge - Environment Configuration
# ===================================================================
# Copy this file to .env.local and fill in your values.
# For automated setup, run: npm run foundry:login
# ===================================================================

# Provider: "mock" (development) or "foundry" (Azure AI Foundry)
VITE_AGENT_PROVIDER=mock

# -------------------------------------------------------------------
# Foundry Connection
# -------------------------------------------------------------------

# Project endpoint (format: https://<resource>.services.ai.azure.com/api/projects/<project>)
VITE_FOUNDRY_ENDPOINT=
VITE_FOUNDRY_BASE_URL=

# Bearer token (Azure AD / Entra). Temporary - refresh via foundry-login.ps1
VITE_FOUNDRY_API_KEY=

# Project identifier (extracted from endpoint automatically by foundry-login.ps1)
VITE_FOUNDRY_PROJECT_ID=

# API version for Azure AI Agent Service (default: v1)
VITE_FOUNDRY_API_VERSION=v1

# -------------------------------------------------------------------
# Foundry Mode
# -------------------------------------------------------------------
# - single-endpoint: one endpoint handles all agent types
# - agent-id: each agent has its own ID, calls go to POST {endpoint}/threads/runs
VITE_FOUNDRY_MODE=single-endpoint

# -------------------------------------------------------------------
# Authentication
# -------------------------------------------------------------------
# - bearer: Authorization: Bearer <token>
# - api-key: Custom header with API key
VITE_FOUNDRY_AUTH_MODE=bearer
VITE_FOUNDRY_API_KEY_HEADER=api-key

# -------------------------------------------------------------------
# Agent IDs (required when VITE_FOUNDRY_MODE=agent-id)
# -------------------------------------------------------------------
# These are the assistant IDs (asst_*) from your Azure AI Foundry project.
# The service calls: POST {endpoint}/threads/runs?api-version=v1
# with body: { "assistant_id": "<agent-id>", "thread": { "messages": [...] } }
VITE_FOUNDRY_AGENT_CONTEXT_INGESTOR_ID=
VITE_FOUNDRY_AGENT_QUESTIONNAIRE_DISCOVERY_ID=
VITE_FOUNDRY_AGENT_REQUIREMENTS_GENERATOR_ID=
VITE_FOUNDRY_AGENT_ACCEPTANCE_CRITERIA_ID=
VITE_FOUNDRY_AGENT_TEST_DESIGN_ID=
VITE_FOUNDRY_AGENT_QUALITY_GATE_ID=
VITE_FOUNDRY_AGENT_VERSIONING_DIFF_ID=
VITE_FOUNDRY_AGENT_EXPORT_ID=
VITE_FOUNDRY_AGENT_AUDIT_LOGGING_ID=

# -------------------------------------------------------------------
# Server-side / Script Variables (used by PowerShell scripts)
# -------------------------------------------------------------------
FOUNDRY_BASE_URL=
FOUNDRY_API_VERSION=v1
FOUNDRY_AZ_RESOURCE=https://ai.azure.com
# FOUNDRY_AZ_CLI_PATH=C:\Program Files\Microsoft SDKs\Azure\CLI2\wbin\az.cmd
